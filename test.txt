cls.main_path = cls._get_env_path('BASE_PATH')
        cls.input_path = cls._get_env_path('INPUTS_PATH')
        cls.tests_path = cls._get_env_path('TESTS_PATH')
        
        cls.mig_mtrx_nms = ["CZ_CORP", "HU_CORP", "WW_GOV"]
        
        cls.data_lake_path = Path(cls.tests_path) / "data" / "mig_mtrxs" / os.getenv('TEST_DATE', 'latest')
        
        cls.calc_dt = "20231231"

    @staticmethod
    def _get_env_path(var_name):
        path_str = os.getenv(var_name, "")
        if not path_str:
            raise RuntimeError(f"The {var_name} environment variable has not been properly configured in the .env file")
        return Path(path_str)
